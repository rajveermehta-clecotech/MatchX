// app.js - Main application initialization
const App = {
    init() {
        document.addEventListener('DOMContentLoaded', () => {
            UI.init();
            console.log('Match X Score App initialized');
        });
    }
};